# Django Settings
SECRET_KEY=your-secret-key-here-change-in-production
DEBUG=True

# Database (para desarrollo local usa PostgreSQL o SQLite)
# Para desarrollo local con PostgreSQL:
DATABASE_URL=postgresql://user:password@localhost:5432/restaurante_db

# Para desarrollo local con SQLite (comentar DATABASE_URL de arriba):
# DATABASE_URL=sqlite:///db.sqlite3

# Render Production (se configura automáticamente en Render)
# DATABASE_URL se proporciona automáticamente por Render

# Hosts permitidos (separados por comas)
ALLOWED_HOSTS=localhost,127.0.0.1

# Para Render en producción agregar:
# ALLOWED_HOSTS=localhost,127.0.0.1,tu-app.onrender.com
